[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[user]
	name = jedl
	email = jed@jedred.com

[url "https://"]
        insteadOf = git://
#[diff]
#	external = /Users/jluhmann/bin/git-diff.sh
[merge]
	tool = opendiff
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = true
  pager = true
